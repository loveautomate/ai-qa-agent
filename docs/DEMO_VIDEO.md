# Demo video files

- **`docs/demo.mp4`** — optional in-repo MP4 linked from the main [`README.md`](../README.md) (plain link + [`demo-poster.svg`](demo-poster.svg) thumbnail). Add this file when you have a recording; until then those links return 404 on GitHub.

**Size:** keep each file **under 100 MB** for standard GitHub pushes, or use **Git LFS** for larger binaries.
